{% extends 'base.html.twig' %}

{% block title %}Blog show {{ id }}!{% endblock %}

{% block body %}
        <p>ID {{ id }}</p>
        <h3>{{ post.title }}</h3>
        <p>{{ post.text }}</p>
{% endblock %}